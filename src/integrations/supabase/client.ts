// This file is automatically generated. Do not edit it directly.
import { createClient } from '@supabase/supabase-js';
import type { Database } from './types';

const SUPABASE_URL =
  import.meta.env.VITE_SUPABASE_URL || 'http://127.0.0.1:54321';
const SUPABASE_PUBLISHABLE_KEY =
  import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY ||
  import.meta.env.VITE_SUPABASE_ANON_KEY ||
  '';
const PROJECT_REF =
  SUPABASE_URL.match(/https:\/\/([^.]+)\.supabase\.co/)?.[1] || 'local';

if (!SUPABASE_URL || !SUPABASE_PUBLISHABLE_KEY) {
  throw new Error(
    '[Supabase] Missing configuration. Set VITE_SUPABASE_URL and VITE_SUPABASE_PUBLISHABLE_KEY or VITE_SUPABASE_ANON_KEY.',
  );
}

if (import.meta.env.DEV) {
  console.log('[Supabase] Configuration:', {
    url: SUPABASE_URL.substring(0, 20) + '***',
    projectRef: PROJECT_REF || 'not detected',
    hasEnvUrl: !!import.meta.env.VITE_SUPABASE_URL,
    hasPublishableKey: !!import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY,
    hasAnonKey: !!import.meta.env.VITE_SUPABASE_ANON_KEY,
  });
}

export const supabase = createClient<Database>(
  SUPABASE_URL,
  SUPABASE_PUBLISHABLE_KEY,
  {
    auth: {
      persistSession: true,
      autoRefreshToken: true,
    },
  },
);

(supabase as any).supabaseUrl = SUPABASE_URL;
